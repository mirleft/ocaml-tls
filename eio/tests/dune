(copy_files ../../certificates/*.crt)
(copy_files ../../certificates/*.key)
(copy_files ../../certificates/*.pem)

(mdx
  (package tls)
  (deps
    server.pem
    server.key
    server-ec.pem
    server-ec.key
    (package tls)
    (package eio_main)
    (package lwt)))
